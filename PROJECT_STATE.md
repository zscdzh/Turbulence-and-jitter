# PROJECT_STATE

更新时间：2026-08-06

本文件是当前项目状态的权威入口。若后续讨论与本文件冲突，应在确认后更新本文件，而不是默认沿用旧结论。

## 1. 项目定位

研究对象为近地面无人机自由空间光通信中的发射光场设计。应用背景同时包含：

- 大气湍流；
- 平台、云台和 PAT/FSM 闭环后的独立机械残余抖动；
- 有限发射口径与有限接收口径；
- 以接收功率低尾和中断概率为核心的链路可靠性。

本项目不把“同时考虑湍流和指向误差”作为创新点。传统联合信道模型、Gaussian 束宽/发散角优化、部分相干 Gaussian 参数优化已是成熟前序。

## 2. 当前最小科学问题

> 独立机械残余抖动是否会改变抗湍流发射光场的性能排序、最优形状和适用域？湍流最优、抖动最优与联合最优光场何时发生分离、排序反转或退化？

进一步的问题是：

> 能否用少数可解释的描述量，预测不同光场在有限孔径接收下的抖动敏感性和联合鲁棒性？

当前优先描述量包括：

- 有限孔径捕获函数；
- 捕获函数中心曲率；
- 给定接收功率门限下的位移覆盖面积；
- 长期光斑尺度；
- halo power fraction；
- 模态分量接收功率相关性。

## 3. 当前路线裁决

### 阶段一：统一评价与适用域

状态：**GO**

目标：

- 明确区分 turbulence-induced beam wander 与 independent residual jitter；
- 建立少量代表性光场的共同仿真与评价模型；
- 研究模式排序是否随湍流、抖动和接收口径发生反转；
- 建立无量纲适用域或相图；
- 判断复杂光场优势是否可被普通 Gaussian 束宽优化解释。

第一阶段只有在得到机制规律、排序反转或明确失效边界时才具有独立论文价值。单纯生成多种光束的 Monte Carlo 曲线不足以成文。

### 阶段二：联合低维光场设计

状态：**CONDITIONAL GO**

只有第一阶段证明非 Gaussian 结构提供不可忽略的联合收益时才推进。

当前候选：

1. flattened-Gaussian / super-Gaussian 阶数—尺度优化；
2. 正交偏振 Gaussian–LG 双模复合光场的功率权重与相对束宽优化。

阶段二的关键不是命名一种“新光束”，而是证明：

- 联合最优参数位于内部区域，而非退化到纯 Gaussian；
- 优势跨连续参数区域存在；
- 优势来源可以被物理量解释；
- 资源代价和适用场景明确。

## 4. 初始模式集合

第一轮不扩展为模式动物园。最小机制集为：

1. 每个信道参数点单独优化的 Gaussian；
2. flattened-Gaussian：`N = 0, 1, 2, 4, 8`；
3. 正交偏振 Gaussian–LG 双模光场；
4. 一个 Bessel–Gaussian 或 OPB 机制对照。

后续模式只有在能减少关键科学不确定性时才加入。

## 5. 当前不做的内容

- 完整无人机六自由度动力学；
- 详细 PAT 控制器和飞控模型；
- 高维像素级相位优化或神经网络；
- 大量 Airy、Pearcey、矢量束、相关束和焦散变体并行扫描；
- 复杂调制、编码和接收机噪声模型；
- 以 CI、代码覆盖率或工程化重构作为研究验收标准。

这些内容可在核心物理结果成立后再评估。

## 6. 评价原则

新方案不要求在所有归一化条件下普适优于 Gaussian。合理结论可以是：

> 在明确场景和系统约束下，以可量化的口径、功率分配或实现复杂度为代价，获得某项显著的低尾可靠性、指向容差或系统简化收益。

但必须满足：

- 总发射功率、实际口径、接收口径和生成效率被报告；
- Gaussian 基线针对同一任务认真优化；
- 优势和代价均透明；
- 不把更宽光斑、更多外围能量或更大口径隐藏成“结构性优势”；
- 不把局部或单点结果外推成普适结论。

## 7. 论文预期

当前合理预期：

- 论文 1：统一评价、排序反转和适用域；成功概率相对较高；
- 论文 2：低维联合光场设计；取决于第一轮仿真是否出现稳定内部最优；
- Frozen Wave：独立支线储备，研究纵向包络、距离变化和有限孔径可靠性，不并入当前主线。

## 8. 下一步

Codex 第一轮只做模型和基准准备，不进行大规模参数扫描：

1. 建立可复现的自由空间传播和圆孔接收功率积分；
2. 建立 Gaussian 位移与抖动解析/数值基准；
3. 实现 flattened-Gaussian 和 Gaussian–LG 双模定义；
4. 明确统一变量、单位、归一化和输出；
5. 给出最小 smoke test 和首轮证伪计划。

详细要求见 `docs/SCIENTIFIC_CONTRACT_DRAFT.md` 与 `docs/CODEX_HANDOFF.md`。
