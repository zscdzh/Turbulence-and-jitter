# AI_RESEARCH_GOVERNANCE

## 1. 目的

本项目由用户负责科学决策，ChatGPT负责问题压缩、理论审查和阶段判断，Codex负责代码实现、数值计算和结果整理。

用户不需要逐行审查代码，但必须持续掌握：

- 科学问题是什么；
- 每个变量代表什么、单位是什么；
- 本轮到底测试了什么；
- 结果支持到哪里；
- 哪些结论仍然禁止；
- 下一步是否仍沿着正确方向推进。

## 2. 每轮负责人可理解层

凡涉及代码、仿真、数值结果或论文判断，默认按以下顺序报告。

### 2.1 负责人摘要

用普通光学、物理和工程语言说明：

1. 上一步已经知道什么；
2. 本轮为什么要做；
3. 实际测试了什么；
4. 关键结果是什么；
5. 对论文主张有什么作用；
6. 下一步是 CONTINUE、REVISE 还是 STOP。

### 2.2 数学定义

首次出现的状态、变量、单位、方向、公式、阈值和归一化方式必须解释。

尤其不得只给出代码变量名，例如 `tau`、`jitter_sigma`、`q05`、`waist_ratio`，而不说明其物理含义。

### 2.3 关键代码链

说明：

- 哪个模块生成发射光场；
- 哪个模块完成自由空间或湍流传播；
- 哪个模块加入机械抖动；
- 哪个模块积分有限孔径功率；
- 哪个脚本执行本轮任务；
- 哪些输出文件是主要证据。

不要求逐行解释代码，但必须给出输入—处理—输出链。

### 2.4 证据入口

列出：

- 运行命令；
- 配置文件；
- 结果表；
- 关键图；
- 随机种子；
- 当前 commit SHA。

### 2.5 结论边界

用以下等级区分：

- **支持**：当前数据直接支持；
- **部分支持**：趋势存在，但参数范围、统计样本或模型尚不足；
- **开放**：尚未测试或证据冲突；
- **禁止**：当前结果明确不能推出。

## 3. 科研而非软件工程的验收原则

本项目不以以下项目作为默认验收要求：

- GitHub Actions CI；
- 高代码覆盖率；
- 全参数穷举；
- 大量工程化异常处理；
- 与科学问题无关的逐文件审计；
- 为形式完整而增加的冗余验证。

最低必要验证包括：

- Gaussian 自由空间传播基准；
- 能量守恒与采样窗口检查；
- 圆孔积分和位移测试；
- 湍流相位屏统计的基本检查；
- 关键参数点的网格或步长收敛；
- 所有候选使用共同随机样本进行配对比较。

验证的目的是保护科学结论，不是把仓库建设成生产软件。

## 4. 禁止的研究推进方式

- 在没有核心科学问题时继续增加光束种类；
- 因某种光束图样漂亮而默认具有通信价值；
- 用闪烁指数降低自动替代低尾功率或 outage 改善；
- 只与未优化 Gaussian 比较；
- 把 turbulence-induced beam wander 与独立机械 jitter 重复计入；
- 在同一批 Monte Carlo 样本上选择参数并报告最终泛化结果；
- 为了得到第二篇论文而人为增加偏振、相干性、OAM 或机器学习自由度；
- 当结果退化为 Gaussian 时继续进行大规模参数扫描挽救方向。

## 5. 分支、PR 与合并

建议 Codex 每个明确科学任务使用独立分支和 Draft PR。

每个 PR 至少包含：

- 科学问题；
- 最小变更范围；
- 变量词典；
- 运行方式；
- 结果摘要；
- 结论边界；
- CONTINUE / REVISE / STOP 建议。

CI 失败不自动阻止科研结果审阅；但代码不能运行、数据不可复现或关键单位错误应阻止合并。

## 6. 当前角色分工

### 用户

- 决定研究方向和是否继续；
- 判断应用场景是否真实；
- 审查结果是否值得形成论文；
- 批准合并和进入大规模计算。

### ChatGPT

- 维持 `PROJECT_STATE.md` 和科学契约；
- 审查研究动机、现有技术、模型和结论；
- 将 Codex 输出翻译为负责人可理解层；
- 识别过度宣称、平凡结果和方向漂移。

### Codex

- 实现模型与脚本；
- 执行经批准的数值任务；
- 输出结构化数据、图和运行记录；
- 不擅自扩大参数范围或改变科学问题；
- 不因软件工程偏好而增加无关工作。
