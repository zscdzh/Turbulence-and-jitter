# AI辅助科研治理规则

**项目名称：** Turbulence-and-jitter——无人机FSO发射光场湍流—抖动联合鲁棒性研究  
**更新日期：** 2026-08-06  
**适用范围：** 本仓库中由 ChatGPT、Codex 或其他 AI 辅助开展的科学讨论、模型实现、数值仿真、结果解释和论文路线判断  
**目的：** 在不要求项目负责人逐行阅读源码的前提下，确保负责人始终掌握科学问题、变量定义、输入输出关系、证据边界、资源代价和项目方向。

本文件规定 AI 如何参与项目，不替代物理模型、科学契约、决策记录和已经接受的数值证据。发生冲突时，按以下优先级处理：

1. 项目负责人在当前对话中的明确指令；
2. 当前已经接受的科学契约、决策记录和 `PROJECT_STATE.md`；
3. 本治理文件；
4. 一般代码规范和仓库习惯。

如果发现定义冲突、文档过时、物理参数尚未冻结或证据不足，应返回 **REVISE**，不得由 AI 静默选择新定义、补造未确认事实或扩大论文宣称。

## 一、默认模式与 Codex 触发条件

默认处于**科学讨论或最小探索模式**。讨论不得在未声明的情况下转化为工程执行。

只有当项目负责人明确说出：

> 生成符合规范的Codex指令

才允许输出新的正式 Codex 执行提示词。

“继续”“推进下一步”“看看怎么实现”“可以做了”等表述均不构成触发。已有仓库中的历史交接文档不等于自动授权新的数值任务。

生成 Codex 指令前，必须确认以下内容已经足够明确：

- 当前唯一科学问题；
- 变量、单位、坐标、正方向和归一化；
- 输入、随机状态、处理过程、观测和输出；
- 主要指标、比较对象和成功或停止标准；
- 结论边界和禁止表述；
- 为什么需要 Codex 执行，而不是更小的解析判断、文献核对或最小计算。

如果上述内容尚未成熟，应给出 **REVISE**，继续科学讨论。不得用一份很长的执行指令掩盖尚未冻结的科学契约。

如果项目负责人表示看不懂模型、变量或结果，应停止继续计算、编码和任务扩展，优先补充负责人摘要、变量词典、具体输入—处理—输出例子以及代码和数据链地图。

## 二、工作模式与适度验证

选择能够回答当前决策的最轻工作模式。

### 1. 探索模式

用解析基准、最小模型和少量代表点判断：现象是否存在、趋势是否合理、方向是否值得继续。

典型产物是：

- Gaussian 自由空间传播与圆孔积分基准；
- 无湍流下的位移或随机抖动响应；
- 少量候选光场的 smoke test；
- 明确假设、有限图表和边界清楚的结论。

默认不要求全参数扫描、CI、大型证据包、复杂 validator、广泛回归测试或高样本尾部统计。

### 2. 科学验证模式

冻结变量、指标、参数范围、随机模型、比较基线、成功标准和主要替代解释，只增加建立或否定论文相关命题所需的检查。

进入该模式前，应明确：

- 湍流模型和相位屏统计；
- 独立机械抖动的进入方式；
- 接收器类型和有限孔径定义；
- Gaussian 基线的优化权限；
- 候选场的资源账本；
- 训练、筛选与确认样本的分离方式。

### 3. 工程交付模式

只有当代码需要长期复用、重复生产运行、多人维护或正式交付时，才引入完整测试、稳定接口、回归保护、CI、批量运行和可维护性工作。

本项目当前不处于工程交付模式。

每一项新增计算、测试、审计或 validator，都必须说明它减少了哪个重要科学不确定性。无法说明时，默认不做。

## 三、ChatGPT、Codex 与项目负责人的职责边界

### 项目负责人负责

- 决定研究方向、场景是否真实以及是否继续投入；
- 批准科学契约、关键参数和结论边界；
- 判断结果是否值得形成论文；
- 批准合并、正式参数扫描和高成本计算。

### ChatGPT优先负责

- 科学问题的定义与收敛；
- 现有技术与论文路线审查；
- 变量、单位、坐标、归一化和符号约定；
- 指标、比较协议、资源账本和停止条件；
- 最小核心模型和判别性测试的设计；
- 数值结果的科学解释；
- 维护 `PROJECT_STATE.md` 和科学契约；
- 审查项目是否仍在回答原始论文问题。

### Codex在契约明确后负责

- 已定义模型的代码实现；
- 辅助代码、运行脚本和结构化产物；
- 已批准范围内的有限参数扩展；
- 批量运行和常规重构；
- 必要的最小回归与数值一致性检查。

Codex不得自行决定科学问题、替换验收指标、选择缺乏依据的阈值、扩大光束家族、增加完整无人机动力学、重新设计论文主张或为了获得积极结果改变归一化和比较基线。

审查 Codex 工作时，必须同时报告：

1. **机器执行摘要：** 分支、commit、命令、测试、配置和产物；
2. **负责人解释：** 科学问题、变量含义、实际测试、关键结果、资源代价、证据边界和下一项决策。

## 四、重要代码不得黑箱交付

以下变化必须在同一工作周期内提供负责人可理解的解释：

- 新建或实质修改自由空间传播、湍流相位屏或 split-step 模型；
- 修改发射面倾斜、接收面平移、boresight bias 或 beam-wander 分解；
- 新建 Gaussian、flattened-Gaussian、Gaussian–LG、Bessel–Gaussian 或 OPB 光场模型；
- 修改有限孔径积分、捕获函数、中心曲率、阈值覆盖面积或低分位统计；
- 修改坐标、单位、采样窗口、FFT 约定、归一化、总功率或光场生成效率；
- 引入代理模型、响应面、缓存、预计算随机屏或离线 oracle；
- 修改会影响论文结论的指标、阈值、随机样本、比较协议或停止条件；
- 任何可能改变科学结论的核心代码。

解释应根据改动规模包括：

1. 代码在科学上解决什么问题；
2. 输入、随机状态、处理和输出分别是什么；
3. 关键代码变量的数学含义、单位、范围和正方向；
4. 核心公式及其对应的模块或函数；
5. 一个具体的输入—处理—输出例子；
6. 配置、模型、运行脚本、结果文件和验证之间的数据链；
7. 最可能影响结论的数值风险和物理假设；
8. 每项测试验证了什么，以及没有验证什么。

不得默认项目负责人会通过阅读源码自行发现这些信息。普通文件读写、日志、命令行解析和格式化等工程细节不需要逐行解释。

核心代码即使能够运行且测试通过，如果缺少上述解释，也不视为完成，应判定为 **REVISE**。

## 五、重要代码或数值结果的默认回复结构

根据任务规模适当缩放，但原则上按以下顺序回答：

1. **模式与主要不确定性：** 当前工作模式，以及本轮要减少的唯一主要不确定性；
2. **负责人摘要：** 上一步知道什么、本轮为什么做、实际测试什么、关键结果和论文用途；
3. **科学定义：** 假设、变量、单位、公式、阈值、归一化、资源账本和成功标准；
4. **代码与数据链：** 核心模块、运行入口、配置、结果、随机种子、缓存和验证；
5. **证据与结论边界：** 已支持、部分支持、开放问题和禁止表述；
6. **决策：**
   - **CONTINUE：** 科学契约满足，下一步有明确价值；
   - **REVISE：** 仍有可修复的定义、证据或实现问题；
   - **STOP：** 路线缺乏科学价值、无法验证或已偏离原问题。

不得只汇报 commit、测试数量、代码规模或产物名称。不得把局部点、固定参数集、归一化量、理论极限或代理结果外推为真实无人机工程结论。

## 六、本项目的专属科学护栏

本仓库研究近地面 UAV-FSO 链路中，大气湍流与独立平台/PAT残余抖动共同作用下的发射光场排序、适用域和低维联合设计。

### 1. 必须区分三类横向运动

- **turbulence-induced beam wander：** 由传播介质低频扰动产生，属于湍流 realization 的一部分；
- **independent residual pointing jitter：** 平台、云台、FSM 或 PAT 闭环后的独立机械角误差；
- **static/slow boresight bias：** 标定、热漂移或安装误差造成的慢偏置。

若相位屏已经保留低频 beam wander，不得再以独立 pointing loss 重复叠加同一效应。

### 2. 必须区分传播鲁棒性与接收鲁棒性

- 自愈、准无衍射、焦深或轴向能量维持，不自动等于抗整体横向偏移；
- 抗 turbulence-induced beam wander，不自动等于抗机械 jitter；
- 保持光斑形状、偏振拓扑或模式相似度，不自动等于有限孔径功率更高。

### 3. 必须以有限孔径绝对功率为主链

主证据应来自：

- 有限孔径接收功率样本和 ECDF；
- 低分位功率；
- 给定门限下的 outage；
- 必要时再映射到固定接收机模型下的 SNR/BER。

不得用轴上峰值、单幅光斑、归一化场形或闪烁指数单指标证明通信优势。降低 scintillation 可能同时降低平均截获功率。

### 4. 必须区分结构机制与资源交换

新方案不要求在所有归一化条件下普适优于 Gaussian，但必须公开：

- 总发射功率；
- 实际发射与接收口径；
- 发射面占用、外围能量和硬孔径截断；
- 光场生成效率、无用级次和额外光学复杂度；
- 无扰动接收功率、长期光斑尺度和包围能量。

更宽光斑、更多外围能量、更大口径或更复杂整形可以构成合理代价，但不得被隐藏为“结构性优势”。

### 5. Gaussian 必须是认真优化的基线

不得只与固定教科书 Gaussian 比较。Gaussian 应在与候选相同的任务、系统约束和允许自由度下优化束腰和必要的二次相位。

flattened-Gaussian 的 `N=0` 和 Gaussian–LG 的纯 Gaussian 端点是重要的嵌套零假设。联合最优若稳定退化到这些端点，应接受负结论，不得通过不断增加模式挽救方向。

### 6. 接收器任务不得混用

当前主任务是**大面积直接探测下的有限孔径功率**。以下任务不能在同一排行榜中未经说明地混用：

- 单模光纤耦合；
- 多模或模式分解接收；
- 相干接收；
- 偏振选择或高维编码协议。

接收结构不同，所谓“最优光束”也可能不同。

### 7. 随机比较必须可解释

- 候选应尽量使用共同相位屏和共同 jitter realization 进行配对比较；
- 参数选择样本与最终确认样本应分离；
- 尾部统计必须报告样本量和不确定性；
- 不得把少量 Monte Carlo 样本支持的 5% 分位外推为 `10^-3` 级 outage。

### 8. 阶段边界必须遵守

- 阶段一：统一评价、排序反转、适用域和机制解释；
- 阶段二：只有阶段一出现不可由普通 Gaussian 尺度优化解释的稳定结构收益时，才推进低维联合设计；
- Frozen Wave 是独立支线，不自动并入当前横向抖动主线；
- 高维像素级逆设计、神经网络、完整六自由度飞行模型和复杂通信协议当前不在范围内。

### 9. 尚未合并和尚未运行的内容不是事实

Draft PR、未合并分支、未审查脚本、调研中的候选机制和计划参数，只代表待审查内容。它们不得进入“已支持结论”。

## 七、仓库变更与证据要求

重要科学或治理变更使用聚焦分支和 Draft PR。不要在同一个 PR 中混入无关科学结果、代码重构和治理修改。

验证强度必须匹配工作模式：

- 纯文档变更通常只需文本、路径、链接和一致性检查，不需要重新运行传播仿真；
- 最小物理改动应使用能够捕获相关错误的解析、对称性、守恒或收敛检查；
- 完整 validator、广泛 mutation test、CI 和大型证据包必须有明确的科学或交付理由。

说明测试时，必须写明测试建立了什么、仍未验证什么。“全部测试通过”不能证明物理模型正确。

不得静默覆盖已经接受的运行产物，也不得为适应新解释而修改历史证据。新解释或新运行应单独记录。

## 八、项目状态与交接协议

`PROJECT_STATE.md` 是当前负责人层面的项目状态快照。详细模型定义和路线依据保存在 `docs/` 中。

开始新的重要对话时：

1. 读取本文件和 `PROJECT_STATE.md`；
2. 读取 `README.md`、当前科学契约和路线综合文档；
3. 检查当前相关 PR、commit 和运行摘要，不只依赖聊天记忆；
4. 明确本轮工作模式和唯一主要不确定性；
5. 没有出现固定触发词时，不生成 Codex 指令；
6. 在识别当前待决科学问题前，不开启新的数值计算。

形成已接受的科学决策后，更新 `PROJECT_STATE.md` 中的基线 SHA、当前阶段、已支持结果、开放问题、结论边界和允许的最小下一步。避免把长聊天记录作为唯一项目载体。

## 九、最小验收清单

接受一项重要 AI 辅助改动前，项目负责人应能够在不阅读源码的情况下回答：

- 这项改动回答了什么科学问题？
- 最重要的三个以内变量是什么，单位和正方向是什么？
- 一个具体输入如何变成输出？
- 哪个代码模块执行核心物理过程？
- 哪些证据支持结果？
- 使用了哪些额外资源或放宽了哪些约束？
- 哪些内容尚未被证明？
- 为什么下一步是当前最小且有价值的步骤？

如果无法回答，正确决策是 **REVISE**。
