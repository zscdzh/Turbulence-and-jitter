# 新ChatGPT对话交接词

请把以下内容作为新对话的首条消息使用。

---

这是 `zscdzh/Turbulence-and-jitter` 科研项目的延续对话。

本项目研究近地面无人机自由空间光通信中，大气湍流与平台/PAT闭环后的独立机械残余抖动共同作用时，发射光场的性能排序、适用域和低维联合设计。

请先通过 GitHub 完整读取当前仓库，不要只依赖本提示词或聊天记忆。读取顺序为：

1. `AI_RESEARCH_GOVERNANCE.md`；
2. `PROJECT_STATE.md`；
3. `README.md`；
4. `docs/SCIENTIFIC_CONTRACT_DRAFT.md`；
5. `docs/LITERATURE_AND_ROUTE_SYNTHESIS.md`；
6. 当前 `main`、开放和已合并 PR、最近提交、运行摘要与决策记录。

读取后，先用项目负责人能够理解的语言给出状态核验，至少说明：

- 当前真正的科学问题；
- 当前阶段和唯一主要不确定性；
- 已经有仓库证据支持什么；
- 哪些内容只是工作假设；
- 当前代码、配置和结果链是否已经建立；
- 当前允许的最小下一步；
- 哪些论文或工程表述仍然禁止。

必须遵守以下边界：

- 不把“同时考虑湍流和pointing error”本身当作创新；
- 区分 turbulence-induced beam wander、independent residual jitter 和 boresight bias；
- 不用轴上峰值、单幅光斑或闪烁指数单指标证明通信优势；
- Gaussian基线必须针对同一任务认真优化；
- 新方案不要求普适最优，但资源代价、适用场景和比较边界必须透明；
- 不把Draft PR、计划参数或待实现模型写成已支持结果；
- 不为获得第二篇论文而自行增加模式、偏振、相干性、OAM、神经网络或完整飞行动力学。

本项目当前预期分为两阶段：

1. 阶段一：代表性光场在湍流—机械抖动—有限孔径下的统一评价、排序反转、适用域和机制解释；
2. 阶段二：只有阶段一出现不可由普通Gaussian束宽优化解释的稳定结构收益时，才推进flattened-Gaussian或正交偏振Gaussian–LG的低维联合设计。

没有出现项目治理文件规定的固定触发词时，不生成Codex执行指令，不直接开启新的数值计算。先完成科学状态核验和当前待决问题判断。

---
